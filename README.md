Transaction with Bitcore
------------------------

Open `presentation/index.html` to view the slides.

Examples are located in the `examples` directory, it may be nessessary to run `npm install` to load the dependencies.